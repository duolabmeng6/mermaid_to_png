# 本地 Mermaid 渲染与图片导出

- 规则：项目定位为个人本地工具，使用 Vue 3、TypeScript、Vite 和 Mermaid 在浏览器内完成解析、预览与导出；没有明确新增需求时不引入后端、数据库或远程渲染服务。
- 规则：核心界面保持中文工作区；单图时为左侧源码、右侧大图预览的双栏结构，多图时在两者之间增加缩略图栏；窄屏切换为上下布局。
- 规则：页面顶部应用标题栏与右侧图片预览标题栏使用约 48px 的单行紧凑布局，隐藏低价值副标题，以直接增加预览画布高度；左侧 Mermaid 编辑器标题栏进一步压缩至约 42px。不能保留 68px 的双行大标题栏。
- 规则：左侧 Mermaid 编辑器提供可键盘操作、带完整无障碍状态的收起/展开按钮；桌面端必须向左横向收起为约 50px 的窄栏，并让右侧预览占用释放出的宽度，不能只减少编辑器高度。小屏单列布局才降级为纵向收起。折叠编辑内容必须保留 textarea DOM、代码、选区、滚动位置和撤销历史。
- 规则：右侧预览必须支持整个面板全屏，且全屏中继续保留主题、背景、缩放、PNG/SVG 导出和大图滚动能力；优先使用浏览器原生 Fullscreen API，不可用或被嵌入环境拒绝时自动降级为铺满窗口的沉浸模式，并支持按钮和 `Esc` 退出。全屏中的导出结果提示必须保持可见；设置工具栏固定为 44px 高的单行布局，空间不足时横向滚动访问全部设置，不能换行挤占画布高度或直接裁切操作项。
- 规则：复杂大图预览不能只强制缩放到一屏；必须提供清晰可见的双向滚动条、适应窗口与明确缩放比例。源码重绘或主题/排版更新时保持当前画板滚动位置和缩放比例，避免无感刷新跳回左上角；用户主动选择“适应窗口”或“回到左上角”时才重置位置。
- 规则：预览画布支持触控板式浏览：普通双指滑动使用原生双轴滚动，捏合手势围绕触点连续缩放并显示当前比例，鼠标主键可拖拽平移；同时保留滚动条、方向键、翻页键及缩放下拉框作为等价操作。不能拦截普通 wheel，也不能用 `transform: scale()` 破坏滚动范围。
- 规则：右侧设置栏提供流程图排版切换，横版使用 Mermaid `LR` 方向，竖版使用 `TD` 方向；切换只改变渲染输入，不改写左侧 Markdown/Mermaid 原文，并统一作用于大图预览、缩略图、PNG/SVG 和全部 ZIP 导出。
- 规则：编辑器验收必须包含真实点击聚焦、逐字输入、删除、撤销和 Tab 缩进；程序化整段 `fill()` 只能验证数据绑定，不能证明用户实际可编辑。
- 规则：编辑器使用 Vue 原生 `v-model` 的输入法组合事件保护；撤销/重做由编辑器维护有界历史（同时限制条数与字符总量），并恢复光标、选区与滚动位置，避免父组件回写破坏原生历史或长文本无限占用内存。预览区的双击编辑、右键增删移动和拖线等可视化操作必须进入同一历史栈；顶部提供明确的“撤回/恢复”按钮，画布非文本焦点支持 Cmd/Ctrl+Z、Cmd/Ctrl+Shift+Z，不能要求用户先回到源码框才能回滚。
- 规则：流程图预览支持双击节点文字直接编辑；键盘聚焦后的 Tab/Enter 用于直接创建下级/同级节点，不能打开编辑器；保存必须精确回写当前 Mermaid 图块、保留节点 ID 与原形状并进入左侧编辑器撤销历史，禁止只修改临时 SVG 或按显示文字全文替换。首版支持普通与手绘 `flowchart` / `graph` 节点、常见括号形状、裸节点及 Mermaid 11 单行 `@{ shape, label }` 语法；空白标签、复杂节点 ID、跨行 metadata、连线文字、子图标题和其他图表类型不得猜测改写，应保留左侧源码编辑入口。
- 规则：流程图预览支持右键菜单完成基础节点 CRUD；节点上右键可查看节点信息、编辑文字、后接并自动连线矩形/圆角/判断/圆形节点，或删除节点及相关连线；画布空白处右键可插入同四类独立节点。增删改必须回写当前 Mermaid/Markdown 图块并进入左侧撤销历史，查看使用只读弹窗；删除仅在能安全识别简单节点 ID、声明、边和节点专属 `style/class/click` 目标时执行，保留 `classDef`、注释和复杂语法，位置由 Mermaid 自动排版，不承诺按右键坐标绝对定位；链接节点、复杂节点、非流程图及错误旧预览保留浏览器原生菜单。键盘可用菜单键或 `Shift+F10` 打开，并支持方向键、Home、End、Tab 与 Esc。
- 规则：`mindmap` 使用独立的层级编辑模型，不显示流程图连接模式；节点双击或键盘 Enter/空格可编辑文字，右键可查看信息、按当前缩进插入子节点、插入同级节点、移动整棵子树到其他父节点或删除整个子树，根节点保留且不可删除。脑图新增、修改、删除和移动均精确回写当前 Mermaid/Markdown 图块并进入左侧撤销历史；节点位置继续由 Mermaid 按缩进自动排版，不承诺自由拖动改变层级。移动操作通过安全对话框选择新父节点，拒绝根节点、自身、后代和当前父节点，避免随意拖动造成层级混乱。
- 规则：脑图层级编辑必须以连续操作闭环验收，不能只验证一次新增或一次删除；反复执行新增子节点、同级节点、删除子树、移动整支和撤销/重做后，父子关系、缩进和完整子树必须保持一致。任何菜单或对话框引用的节点身份在源码重绘后失效时必须关闭或重新解析，禁止继续用旧节点序号操作新树。
- 规则：脑图节点解析必须遵循 Mermaid 的实际语义；例如 `预算(含税)`、`方案[A]` 会被 Mermaid 解释为带节点标识的形状，不能把括号外前缀误当成可见标签。通过可视化编辑写入包含括号、方括号等结构符号的普通文字时，必须自动使用安全的带引号矩形包裹，确保画布显示完整文字。删除失败提示需区分根节点保护与普通节点定位失败，避免把可编辑节点误报为根节点。
- 规则：SVG 直接以矢量文件导出；PNG 复用 Mermaid 官方 Live Editor 的 SVG → Image → Canvas 路线，提供 1–4 倍清晰度、最终像素提示、背景选择和 Canvas 尺寸保护。
- 规则：PNG 导出提供统一的四周留白设置，默认每边 32px，可选 0、16、32、48、64px；留白随清晰度倍率同比缩放，单张 PNG 与 ZIP 内全部 PNG 使用同一设置并保存到当前浏览器。透明背景的留白保持透明，主题色或白色背景铺满整张画布；SVG 保持原始矢量边界不受影响。
- 规则：图表主题使用集中式预设配置；提供 Mermaid 11.17.2 支持的 11 个官方主题和 4 个基于官方 `base` 主题变量机制的精选主题。主题选择需按经典、现代、多彩、精选分组，显示当前三色预览，并与本地设置持久化、预览画布和 PNG/SVG 导出背景保持一致。
- 规则：深色主题选择纯白或透明背景时必须提示可读性风险；`themeVariables.background` 只参与 Mermaid 配色推导，实际导出背景仍由 SVG 背景矩形或 PNG Canvas 填充负责。
- 规则：Mermaid 使用严格安全模式；渲染需要防抖、串行队列和 revision 校验，语法错误时保留上一次成功预览但禁用导出。
- 规则：实时重绘必须保持已有大图可见且可缩放、拖移，只有首次尚无可用预览时才显示居中加载遮罩，禁止每次输入都用模糊层盖住旧图。`mermaid.render()` 已包含语法解析，不得在同一次渲染前重复调用 `mermaid.parse()`。多图缩略图按“源码＋主题＋排版”缓存并只重绘变化项，插入、删除、重排时按完整 key 复用，重复图在同一轮只渲染一次；失败结果必须明确显示失败，不能冒充旧缩略图。
- 规则：编辑区同时兼容纯 Mermaid 源码和包含多个 fenced `mermaid` 代码块的 Markdown；不能把整篇 Markdown 直接交给 Mermaid 解析。只有识别到多张图时，才在编辑器与大图预览之间显示真实渲染的可选缩略图栏；缩略图存在 Markdown 标题时显示标题，不存在时不补可见标题。单图时隐藏缩略图栏。右侧设置栏不得再显示上一张、下拉选择和下一张控件。
- 规则：实时预览与批量导出必须共用模块级 Mermaid 串行渲染队列，避免全局 Mermaid 实例的 initialize、parse、render 在不同任务之间互相覆盖。
- 规则：Mermaid 11.17 必须通过顶层 `htmlLabels: false` 生成纯 SVG 文本；已弃用的 `flowchart.htmlLabels` 会被顶层默认值遮蔽。导出前还需将 HTML 风格的 `<br>`、`<img>` 和常见空白实体转换为 XML 安全格式，避免尺寸解析失败或 `foreignObject` 污染 Canvas。
- 规则：草稿与设置只保存在当前浏览器；存储不可用时必须明确提示，不能声称已经保存。
- 规则：多图批量导出使用浏览器本地生成 PNG 后打包 ZIP；单张失败不阻断其他图片，ZIP 内写入失败清单。首版一次最多 50 张且累计 PNG 最多 200 MB，超限时提示降低清晰度或分批导出；ZIP 使用轻量 `fflate`，不自实现归档格式。
- 规则：线上版本使用 GitHub Pages 托管，`main` 分支推送后由 `.github/workflows/deploy-pages.yml` 自动执行 pnpm 安装、测试、生产构建和部署；生产资源基路径固定为 `/mermaid_to_png/`，本地开发仍使用 `/`。
- 规则：项目统一使用 `pnpm@11.11.0` 和 `pnpm-lock.yaml`；`node_modules`、`dist`、`.DS_Store` 与 `package-lock.json` 不进入仓库。
- 规则：公开入口为 `https://duolabmeng6.github.io/mermaid_to_png/`；该 GitHub 账号配置了 Pages 自定义域名，因此标准入口会跳转到 `https://my.rongyiapi.com/mermaid_to_png/`，不要为本项目擅自移除账号级自定义域名。
- 规则：仓库 README 顶部必须直接展示可点击的自定义域名使用地址，并在 `docs/images/` 保存一张可点击的桌面双栏主界面截图，方便 GitHub 访客无需安装即可理解和进入工具。
- 规则：线上页面顶部操作区必须提供可点击的 GitHub 仓库入口 `https://github.com/duolabmeng6/mermaid_to_png`；桌面端显示图标和文字，窄屏保留带无障碍名称的紧凑图标按钮。
- 规则：验收报告必须明确区分静态验证与浏览器端到端验证；只有实际完成代码粘贴、预览生成、错误恢复和 PNG/SVG 下载文件检查，才能声称核心功能已经完整测试。
- 静态验证证据：`pnpm test` 12 项通过，`pnpm run build`（含 vue-tsc）通过，本地 Vite HTTP 响应正常；11 个 Mermaid 官方主题均通过当前安装版本的运行时配置冒烟检查。这些证据本身不代表浏览器交互与下载链路已通过。
- 浏览器验收证据：使用用户提供的 40 节点、39 条边、7 子图复杂流程图完成真实交互实测；编辑区可视尺寸 489 × 458、内容范围 1497 × 1503，逐字中文输入、Backspace、Cmd/Ctrl+Z、重做、Tab 和错误恢复均通过。预览 100% 模式可视尺寸 689 × 424、内容范围 2777 × 2201，键盘实际滚动至 240/339（增强横移实测至 480）并可一键归零；适应窗口、50%、75%、100%、125% 五档尺寸均通过。3× PNG 实际文件为 8140 × 6413，最终 SVG 通过 XML 校验且 `foreignObject=0`；孤立 `end` 错误提示、保留旧预览、禁用导出及恢复后重新启用均通过。
- 发布验证证据：首次推送提交 `c9816c3` 后启用 Pages 的 GitHub Actions 来源，重新运行部署的 build 与 deploy 作业均成功；GitHub API 返回 `has_pages: true`，标准入口及自定义域名入口均返回 HTTP 200，页面标题为“Mermaid 图像工坊”，线上 JS/CSS 资源使用 `/mermaid_to_png/assets/` 路径。
- Markdown 多图验证证据：用户提供的 209 行样例实际识别出 9 个 Mermaid 图表及 9 个对应二级标题；22 项 Vitest 测试通过，包含纯 Mermaid 回退、多围栏提取、异常围栏、安全文件名和中文 ZIP 实际解包；`vue-tsc` 与生产构建通过，本地 Vite 服务返回 HTTP 200。按项目约束未执行浏览器自动化交互验收。
- 缩略图栏静态验证证据：多图时按“编辑器 → 真实 SVG 缩略图栏 → 大图预览”渲染，单图通过条件渲染移除缩略图栏；原上一张、文字下拉和下一张代码已清理。缩略图批量渲染复用全局串行队列并增加 320ms 防抖，窄屏改为横向滚动；22 项测试、`vue-tsc` 和生产构建通过，本地服务返回 HTTP 200。未执行浏览器自动化交互验收。
- 排版切换静态验证证据：右侧设置栏已增加“跟随代码 / 横版 / 竖版”，统一作用于大图、缩略图、PNG/SVG 和全部 ZIP，且不改写编辑区原文；方向转换覆盖深缩进子图、frontmatter、BOM、CRLF、分号语句和多行 Markdown 标签保护。31 项 Vitest 测试、`vue-tsc`、生产构建和只读代码复核通过，本地服务返回 HTTP 200。按项目约束未执行浏览器自动化交互验收。
- 触控板式预览静态验证证据：连续缩放使用实际图形区域计算触点锚点，细粒度 wheel 增量保留浮点精度，鼠标拖移使用 Pointer Capture 并覆盖抬起、取消、失去捕获和卸载清理。35 项 Vitest 测试、`vue-tsc` 和只读代码复核通过；按项目约束未执行浏览器自动化交互验收，真实触控板手感由用户在本地页面验收。
- PNG 留白静态验证证据：导出画布尺寸、背景填充和图表绘制偏移统一由共享 PNG 导出函数处理，并覆盖单张与 ZIP；留白后的 Canvas 边长仍受浏览器安全上限约束。36 项 Vitest 测试、`vue-tsc` 和生产构建通过；按项目约束未执行浏览器自动化交互验收。
- 节点双击编辑静态验证证据：Mermaid 11.17 DOM ID 映射、常见与 metadata 节点形状、重复定义、裸节点、特殊文字转义、空白值拒绝、Markdown 第二图精确替换和 CRLF 保留均有测试覆盖；51 项 Vitest 测试、`vue-tsc`、生产构建及只读代码复核通过。按项目约束未执行浏览器自动化，真实双击、对话框焦点和键盘手感由用户在本地页面验收。
- 右键插入节点静态验证证据：节点/空白菜单分流、四种节点源码生成、自动连线、子图缩进、连续 ID 避让、特殊文字转义、单行分号流程图、Markdown 第二图精确插入和 CRLF 保留均已覆盖；60 项 Vitest 测试、`vue-tsc`、生产构建、只读代码复核及本地 Vite HTTP 200 检查通过。按项目约束未执行浏览器自动化，真实右键、全屏菜单位置与键盘焦点手感由用户在本地页面验收。
- 节点 CRUD 静态验证证据：右键菜单新增只读查看、删除确认及删除回写；删除覆盖普通/分号语句、相关边、节点专属样式、`classDef`/注释保留、标签同名文字防误删、非节点和无效 ID 拒绝，增删改均复用 Markdown 图块替换与撤销链路。63 项 Vitest 测试、`vue-tsc`、生产构建及本地 Vite HTTP 200 检查通过；按项目约束未执行浏览器自动化，真实菜单、确认弹窗和只读信息弹窗由用户在本地页面验收。
- 流畅重绘静态验证证据：主预览更新期间保留旧图且继续允许缩放、拖移，居中加载遮罩仅用于首次无图状态；重复解析已移除，缩略图按完整 key 增量缓存、跨位置复用、同轮去重并正确处理失败和 revision 过期结果。60 项 Vitest 测试、`vue-tsc`、生产构建、两轮只读代码复核及本地 Vite HTTP 200 检查通过。按项目约束未执行浏览器自动化，连续输入和多图切换手感由用户在本地页面验收。
- 无感刷新静态验证证据：预览组件持续记录双轴滚动值，SVG 更新后在 DOM 完成替换的下一帧恢复原位置；主动“适应窗口”和“回到左上角”仍会清零。60 项 Vitest 测试、`vue-tsc`、生产构建和本地 Vite HTTP 200 检查通过；按项目约束未执行浏览器自动化，实际编辑时的画板位置由用户在本地页面验收。
- 规则：流程图预览支持从可编辑节点拖到另一个可编辑节点创建 `-->` 连线；拖动超过 5px 后才激活并显示临时虚线，避免误触双击编辑，松开到空白或无效目标时取消。拒绝自连接、重复边、复杂节点 ID 和非流程图；连线通过当前 Mermaid/Markdown 图块安全回写并进入左侧撤销历史，节点位置继续由 Mermaid 自动排版，不承诺绝对坐标拖动。
- 规则：流程图预览中的可识别连线支持右键打开连线菜单并单独删除，删除时保留两端节点和其他边、回写当前 Mermaid/Markdown 图块并进入左侧撤销历史；连线使用加宽透明命中区方便选择，并支持键盘聚焦后按菜单键或 `Shift+F10` 操作。新增连线只拒绝自连接、重复边、无效节点和不支持的复杂 ID；已有路径或回路允许建立，以保证连接、删除、撤销形成完整闭环，用户可通过右键删除或左侧撤销处理不需要的边。复杂链式/并行语句无法安全拆分时拒绝删除并提示编辑源码。
- 规则：可视化编辑必须提供恢复闭环。对能由用户撤销或删除的有效连线，不得仅因可能产生冗余路径、回路或重新排版而禁止；连线后应明确说明 Mermaid 会自动排版，并同时提示右键删线和左侧撤销，避免用户只能修改却不能恢复。
- 规则：流程图连接模式必须明确“从起点拖到终点，箭头指向松开位置”；方向决定 Mermaid 的语义关系和自动层级，接回上游会形成回路并显著改变布局。允许用户建立有效回路以保持编辑闭环，但界面提示需说明回路会改变布局，并在成功提示中显示实际 `起点 → 终点`。
- 拖拽连线静态验证证据：`insertFlowchartEdge` 覆盖流程图校验、节点存在、自环/重复边拒绝、子图缩进、标签同名防误判、Markdown 图块及 CRLF/CR 回写；`pnpm test` 66 项通过、`vue-tsc`、生产构建和本地 Vite HTTP 200 检查通过。未执行浏览器自动化，真实拖拽手感、临时线显示和目标高亮由用户在本地页面手工验收。
- 连线删除静态验证证据：Mermaid 实际带图表前缀的边 DOM ID 映射、普通边和带文字边删除、分号语句、注释、两端节点保留、CRLF/CR 及复杂链式语句拒绝均有测试覆盖；`pnpm test` 71 项、`vue-tsc`、生产构建及本地 Vite HTTP 200 检查通过。早期测试只使用裸 `L_A_B_0`，未覆盖实际的 `mermaid-diagram-...-L_A_B_0`，曾导致连线未注册且右键菜单不可见，现已纠正；按项目约束未执行浏览器自动化，真实连线命中与右键菜单手感由用户在本地页面验收。
- 浏览器验收证据：在本地 `http://localhost:5175/` 真实拖拽 B 到 A 后，源码成功写入 `B --> A`；拖动过程中观察到临时连线和 `.is-edge-dragging` 状态，悬停目标出现 `.is-edge-drop-target` 高亮；拖到空白处及重复拖拽均保持源码不变。测试结束后恢复原始源码并重新加载，页面显示 40 个可编辑节点、无空状态覆盖，未出现新的控制台错误。随后 `pnpm test` 66 项、`vue-tsc`、生产构建和 HTTP 200 均通过。
- 浏览器验收证据（2026-09-04）：使用用户提供的 14 节点样例实测默认模式拖动不会改写源码；连接模式下 `c1→c14`、`c2→c10` 等已有路径的冗余长边现在可以建立，重复边仍被拒绝；最小图 `B→C` 合法连线可建立，随后通过连线右键菜单删除该边，源码恢复且删除提示可见。补充 `index.html` 的 `#preview-toast-host` 挂载点后，成功与失败 Toast 均能显示；页面无新增控制台错误。
- 脑图交互验证证据（2026-09-04）：使用 8 节点 `mindmap` 实际完成双击编辑、右键插入矩形子节点、右键删除子树、根节点删除保护，以及左侧撤销/重做；流程图同时完成冗余连线建立、重复边提示和右键删除。页面控制台错误数为 0，`pnpm test` 80 项、`vue-tsc` 与生产构建通过。
- 脑图层级闭环验证证据（2026-09-04）：在本地页面实际完成右键新增同级节点、将带子节点的整支移动到其他父节点、移动后撤销/重做、删除确认显示下级数量，以及根节点删除按钮 disabled；源码缩进和子树均保持正确。静态验证 `pnpm test` 97 项、`pnpm run typecheck` 和 `pnpm run build` 均通过，页面恢复为原始流程图且无错误提示。
- 脑图连续增删纠错证据（2026-09-04）：确认旧实现删除位于源码末尾且带后代的分支时，只删父节点行并把残留后代误挂到上一层；现改为按物理行扫描完整子树边界，删除所有更深缩进的后代及子树注释，同时保留同级/上级尾部内容。新增“末尾子树删除后继续新增”和“保留根级尾注释”回归测试；`pnpm test` 99 项、`pnpm run typecheck`、`pnpm run build` 与只读复核通过。按当前对话约束未执行浏览器自动化。
- 操作级撤回入口验证证据（2026-09-04）：保留编辑器原有有界历史栈并通过组件公开方法接入顶部“撤回/恢复”按钮；预览区非文本焦点支持 Cmd/Ctrl+Z、Cmd/Ctrl+Shift+Z，文本输入焦点仍交给 textarea 原生历史，避免抢占逐字编辑。`pnpm test` 99 项、`pnpm run typecheck` 和 `pnpm run build` 通过；未执行浏览器自动化，按钮和快捷键手感由用户在本地页面验收。
- 箭头方向提示验证证据（2026-09-04）：根据用户截图确认 `1 → 2 → 3` 与新增 `3 → 1` 的回路会触发 Mermaid 重新排版；连接按钮标题、连接模式底部说明和成功 Toast 已明确起点、终点及箭头指向，并提示回路会改变布局。`pnpm test` 99 项、`pnpm run typecheck`、`pnpm run build` 和本地 HTTP 200 通过；未执行浏览器自动化。
- 规则：浏览器自动化验收只在当前对话获得用户明确授权后执行；获授权后可由 Agent 自行接管独立测试空间，验收结束按需显式保留或关闭页面。
- 规则（覆盖上方脑图快捷键旧描述）：聚焦脑图节点后，Tab 新增子节点，Enter 新增同级节点；根节点按 Enter 时回退为新增子节点；空格和双击继续编辑文字。键盘新增复用节点编辑对话框，填写文字后点击插入或按 Cmd/Ctrl+Enter 确认。
- 规则（焦点闭环）：脑图快捷键打开节点编辑/插入对话框后，取消、Esc、保存和 SVG 重绘完成都必须把焦点恢复到触发节点；触发节点已经不存在时回到预览画布，不能落到页面其他工具栏控件。
- 规则（最新键盘语义，覆盖上方旧描述）：脑图聚焦节点后，Tab 直接创建下一级“新节点”，Enter 直接创建同级“新节点”（根节点 Enter 仍创建子节点）；创建后自动聚焦新节点，便于连续操作。脑图文字只通过双击编辑，空格不再打开编辑器；右键菜单编辑入口继续保留。
- 规则（节点获焦）：SVG 节点在鼠标点击时必须显式获得页面焦点，并设置 SVG `focusable` 标记；只有先聚焦节点，Tab/Enter 才执行脑图层级快捷键，空白页面按 Tab 仍保留浏览器正常导航。
- 验证证据（2026-09-04）：键盘快捷键分支增加重绘期间交互新鲜度校验，并同步根节点例外、流程图空格编辑及底部/无障碍提示文案；`pnpm test` 99 项、`pnpm run typecheck`、`pnpm run build` 通过，按当前对话约束未执行浏览器自动化。
- 验证证据（2026-09-04）：针对用户反馈的 Tab 焦点乱跳，新增触发节点记忆、重绘后下一帧焦点恢复及节点失效时画布兜底；`pnpm test` 99 项、`pnpm run typecheck`、`pnpm run build` 和本地 HTTP 200 通过，实际连续 Tab 手感仍需用户在浏览器中验收。
- 验证证据（2026-09-04）：针对用户反馈 Tab 直接落入浏览器地址栏，新增 SVG 节点点击显式聚焦和 `focusable=true`，并保持连接模式/画布拖动分支不变；`pnpm test` 99 项、`pnpm run typecheck`、`pnpm run build` 通过，未执行浏览器自动化。
- 验证证据（2026-09-04）：按用户最新确认将脑图 Tab/Enter 改为直接创建并在重绘后聚焦新节点，空格不再编辑、双击保留文字编辑；`pnpm test` 99 项、`pnpm run typecheck`、`pnpm run build` 通过，未执行浏览器自动化。
- 规则（用户纠正，覆盖仅脑图快捷键范围）：流程图也必须遵循统一键盘语义；聚焦节点后，Tab 直接创建并连接一个下级节点，Enter 直接创建同级节点，不得打开文字编辑器；文字编辑仅由双击触发。流程图同级关系按箭头入边推导：复制当前节点的入边到新节点；没有入边时创建独立节点。空格不打开编辑器。
- 验证证据（2026-09-04）：用户明确反馈其实际测试的是 `flowchart TD`，上一版仅实现脑图快捷键导致 Tab 进入浏览器焦点链、Enter 打开当前节点编辑；本次按该纠正扩展流程图键盘入口，纯函数测试 102 项通过，且在本地页面用真实鼠标坐标 + Tab/Enter 验证 A→下级、B→同级、带文字箭头入边和双击编辑，控制台 error/warn 为 0。
- 来源：用户需求、用户纠正、互联网调研与已验证实现。
- 任务锚点：直接对话
- 首次记录：2026-08-28
- 最后确认：2026-09-04
